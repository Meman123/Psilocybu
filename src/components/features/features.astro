---
import { Image } from 'astro:assets';
// Asegúrate de que los nombres de archivo coincidan con los que guardaste en src/assets
import featureImage from '@/assets/growkit-psilocybu.jpg';
import logoBadge from '@/assets/Logo.png';
---

<style>
  .features-section-v2 {
    /* Tailwind: py-16 md:py-24 bg-white */
    background-color: var(--color-background);
    padding-top: var(--spacing-xl);
    padding-bottom: var(--spacing-xl);
  }

  .container {
    max-width: 1280px;
    margin-left: auto;
    margin-right: auto;
    padding-left: var(--spacing-md);
    padding-right: var(--spacing-md);
  }

  .content-wrapper {
    /* Tailwind: flex flex-col md:flex-row items-center gap-12 */
    display: flex;
    flex-direction: column; /* Mobile-first: stacked vertically */
    align-items: center;
    gap: var(--spacing-xl);
  }

  .image-column {
    /* Tailwind: md:w-1/2 relative */
    width: 100%;
    position: relative;
  }

  .main-image {
    /* Tailwind: rounded-lg shadow-2xl w-full */
    border-radius: var(--border-radius-medium);
    box-shadow: 0 25px 50px -12px rgb(0 0 0 / 0.25);
    width: 100%;
    height: auto;
  }

  .logo-badge {
    /* Tailwind: absolute top-4 left-4 bg-white p-2 rounded-md shadow-lg */
    position: absolute;
    top: var(--spacing-sm);
    left: var(--spacing-sm);
    background-color: var(--color-background);
    padding: var(--spacing-xs);
    border-radius: 6px;
    box-shadow: var(--box-shadow-subtle);
  }

  .logo-badge img {
    height: 32px;
    width: auto;
  }

  .text-column {
    /* Tailwind: md:w-1/2 text-center md:text-left */
    width: 100%;
    text-align: center;
  }

  .headline {
    /* Tailwind: text-4xl md:text-5xl font-bold ... leading-tight */
    font-size: 2.5rem;
    font-weight: var(--font-weight-bold);
    color: var(--color-text);
    margin-bottom: var(--spacing-sm);
    line-height: 1.2;
  }

  .headline .highlight {
    color: var(--color-accent);
  }

  .subheadline {
    /* Tailwind: text-xl text-gray-600 mb-8 */
    font-size: 1.25rem;
    color: #4a5568;
    margin-bottom: var(--spacing-lg);
  }

  .features-list-box {
    /* Tailwind: bg-gray-100 p-6 rounded-lg shadow-inner */
    background-color: #f7fafc;
    padding: var(--spacing-lg);
    border-radius: var(--border-radius-medium);
    box-shadow: inset 0 2px 4px 0 rgb(0 0 0 / 0.05);
  }

  .features-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }
  
  .feature-item {
    /* Tailwind: flex items-start */
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-sm);
    text-align: left;
  }

  .feature-item .icon {
    font-family: 'Material Icons';
    color: var(--color-accent);
    margin-top: 4px; /* Align icon with text */
  }

  .feature-item h3 {
    font-weight: var(--font-weight-semibold);
    color: var(--color-text);
  }

  .feature-item p {
    font-size: 0.875rem;
    color: #4a5568;
    line-height: 1.5;
  }

  .cta-buttons {
    margin-top: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .btn {
    font-weight: var(--font-weight-bold);
    padding: 12px 32px;
    border-radius: 9999px; /* rounded-full */
    box-shadow: var(--box-shadow-subtle);
    transition: all 0.3s ease;
    text-align: center;
    font-size: 1.125rem;
  }

  .btn:hover {
    transform: scale(1.05);
  }
  
  .btn-primary {
    background-color: var(--color-accent);
    color: var(--color-text);
  }

  .btn-secondary {
    background-color: var(--color-background);
    color: var(--color-text);
    border: var(--border-width-thin) solid #cbd5e0;
  }

  /* --- Responsive Styles --- */
  @media (min-width: 768px) {
    .features-section-v2 {
      padding-top: calc(var(--spacing-xl) * 2);
      padding-bottom: calc(var(--spacing-xl) * 2);
    }
    .content-wrapper {
      flex-direction: row; /* Two columns on desktop */
    }
    .image-column, .text-column {
      width: 50%;
    }
    .text-column {
      text-align: left;
    }
    .headline {
      font-size: 3rem;
    }
    .cta-buttons {
      flex-direction: row;
      justify-content: flex-start;
    }
  }
</style>

<section class="features-section-v2">
  <div class="container">
    <div class="content-wrapper">
      <div class="image-column">
        <Image 
          src={featureImage} 
          alt="GrowKit de PSILOCYBU en una mesa de madera con fondo hogareño desenfocado"
          class="main-image"
          format="webp"
          quality={80}
        />
        <div class="logo-badge">
          <Image 
            src={logoBadge}
            alt="Logo pequeño de PSILOCYBU"
            format="webp"
            quality="max"
          />
        </div>
      </div>
      <div class="text-column">
        <h1 class="headline">¡Cosecha tus honguitos en solo <span class="highlight">2 semanas!</span></h1>
        <p class="subheadline">Sin experiencia, sin equipos caros, solo 5 minutos a la semana.</p>
        <div class="features-list-box">
          <ul class="features-list">
            <li class="feature-item">
              <span class="icon">check_circle</span>
              <div>
                <h3>Listo para usar</h3>
                <p>Incluye micelio maduro en #bagtek + atomizador, clip e instructivo.</p>
              </div>
            </li>
            <li class="feature-item">
              <span class="icon">watch_later</span>
              <div>
                <h3>Fácil y Rápido</h3>
                <p>Solo 10 atomizadas cada 5 días. ¡Así de simple!</p>
              </div>
            </li>
            <li class="feature-item">
              <span class="icon">verified_user</span>
              <div>
                <h3>100% Seguro y Accesible</h3>
                <p>Cultivo de Psilocybe cubensis var. Albino Averys: máxima potencia y calidad a bajo costo.</p>
              </div>
            </li>
             <li class="feature-item">
              <span class="icon">eco</span>
              <div>
                <h3>Conecta con la naturaleza</h3>
                <p>Ideal para adultos que buscan autocultivar sin experiencia previa.</p>
              </div>
            </li>
          </ul>
        </div>
        <div class="cta-buttons">
          <a href="#" class="btn btn-primary">Compra tu GrowKit aquí</a>
          <a href="#" class="btn btn-secondary">Ver cómo funciona</a>
        </div>
      </div>
    </div>
  </div>
</section>