---
import Layout from '@/layouts/Layout.astro';
---
<Layout title="Psilocybu – Pago exitoso">
  <main class="status-container">
    <div class="status-card">
      <!-- Ícono de check -->
      <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="#22c55e" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="10" />
        <path d="M8 12l2.5 2.5L16 9" />
      </svg>

      <h1 class="status-title success">¡Gracias por tu compra!</h1>
      <p class="status-message">
        Tu pago se procesó correctamente.  
        Pronto recibirás un correo con los detalles de tu pedido.
      </p>

      <a href="/" class="status-button">Volver al inicio</a>
    </div>
  </main>
</Layout>

<style>
  /* Contenedor principal */
  .status-container{
    display:grid;
    place-items:center;
    min-height:calc(100vh - var(--header-height-mobile));
    padding:var(--spacing-lg);
  }
  @media(min-width:768px){
    .status-container{min-height:calc(100vh - var(--header-height-desktop));}
  }

  /* Tarjeta */
  .status-card{
    background:var(--color-background-alt);
    border-radius:var(--border-radius-medium);
    padding:var(--spacing-xl);
    max-width:540px;
    width:100%;
    text-align:center;
    box-shadow:var(--box-shadow-subtle);
    display:flex;
    flex-direction:column;
    gap:var(--spacing-lg);
    align-items:center;
  }

  /* Título */
  .status-title{
    font-size:2rem;
    font-weight:var(--font-weight-bold);
    margin:0;
    text-shadow: 0 2px 2px rgba(0,0,0,0.25), 0 0 4px #222;
  }
  .status-title.success{color:var(--color-accent);} /* amarillo marca */
  .status-title.pending{color:#f59e0b;}             /* naranja */
  .status-title.failure{color:#ef4444;}             /* rojo */

  /* Mensaje */
  .status-message{
    font-size:1.1rem;
    color:var(--color-text);
    margin:0;
  }

  /* Botón */
  .status-button{
    display:inline-block;
    background:var(--color-accent);
    color:var(--color-text);
    padding:var(--spacing-sm) var(--spacing-lg);
    border-radius:var(--border-radius-medium);
    font-weight:var(--font-weight-semibold);
    transition:transform .2s ease;
  }
  .status-button:hover{transform:scale(1.05);}
</style>
